[pytest]
testpaths = tests
addopts   = -v --tb=short
filterwarnings =
    # netCDF4 triggers a NumPy 2.5 shape-assignment deprecation via its C layer
    ignore:Setting the shape on a NumPy array has been deprecated:DeprecationWarning
