#
# working directory in specific disk space depending on the system.
export Rundir='/cluster/work/users/xiejp/ERA5_hourly'

for ii in download 6h hourly; do
   [ ! -r ${Rundir}/${ii} ] && mkdir ${Rundir}/${ii}
done
